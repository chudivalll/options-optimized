/**
 * Database seeding tool
 *
 * Usage: npm run seed
 *
 * This is a symlink reference — the actual seed script lives at prisma/seed.ts.
 * It loads modules, lessons, and scenario JSON files into the SQLite database.
 *
 * To add new data:
 * - Modules/Lessons: Edit src/data/modules.ts and src/data/lessons.ts
 * - Scenarios: Add JSON files to src/data/scenarios/
 * - Then run: npm run seed
 */

export {};
console.log("Run 'npm run seed' from the project root to seed the database.");
console.log("The seed script is at prisma/seed.ts");
